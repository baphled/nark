#!/usr/bin/env ruby

$LOAD_PATH.unshift(File.join(File.dirname(__FILE__), '..', 'lib'))
require 'nark'

method = ARGV.shift
plugin_name = ARGV.shift
Nark::Plugin.public_send method.to_sym, plugin_name
