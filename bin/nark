#!/usr/bin/env ruby

$LOAD_PATH.unshift(File.join(File.dirname(__FILE__), '..', 'lib'))
require 'nark'

method = ARGV.shift
arguement = ARGV.shift
if method == 'list'
  puts Nark::Plugin.list(arguement)
else
  Nark::Plugin.public_send method.to_sym, arguement
end
